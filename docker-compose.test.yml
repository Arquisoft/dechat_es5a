version: '3.3'

services:
  web:
    container_name: dechat-app-container.test
    image: lamasape/dechat-app.test
#    image: dechat-app.test
#    build:
#      context: .
#      dockerfile: Dockerfile.test
    volumes:
      - '.:/app'
      - '/app/node_modules'